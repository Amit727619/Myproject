<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=<device-width>, initial-scale=1.0" />
        <title>Document</title>
        <style>
            .topnav {
                overflow: hidden;
                background-color: rgb(202, 64, 113);
            }

            .topnav a {
                float: left;
                color: #f2f2f2;
                text-align: center;
                padding: 14px 16px;
                text-decoration: none;
                font-size: 17px;
            }

            .topnav a:hover {
                background-color: #ddd;
                color: black;
            }

            .topnav a.active {
                background-color: pink;
                color: white;
            }

            .topnav-right {
                float: right;
            }

            body {
                font-family: "Lato", sans-serif;
            }

            .sidebar {
                height: 100%;
                width: 230px;
                position: fixed;
                z-index: 1;
                top: 0;
                left: 0;
                background-color: rgb(202, 64, 113);
                overflow-x: hidden;
                padding-top: 16px;
            }

            .sidebar a {
                padding: 6px 8px 6px 16px;
                text-decoration: none;
                font-size: 20px;
                color: white;
                display: block;
            }

            .sidebar a:hover {
                color: #f1f1f1;
            }

            table {
                margin-left: 270px;
            }

            .main {
                margin-left: 230px; /* Same as the width of the sidenav */
                padding: 15px 10px;
            }

            @media screen and (max-height: 450px) {
                .sidebar {
                    padding-top: 15px;
                }
                .sidebar a {
                    font-size: 18px;
                }
            }

            #customers {
                font-family: Arial, Helvetica, sans-serif;
                border-collapse: collapse;
                width: 100%;
            }

            #customers td,
            #customers th {
                border: 1px solid #ddd;
                padding: 8px;
            }

            #customers tr:nth-child(even) {
                background-color: #f2f2f2;
            }

            #customers tr:hover {
                background-color: #ddd;
            }

            #customers th {
                padding-top: 12px;
                padding-bottom: 12px;
                text-align: left;
                background-color: rgb(202, 64, 113);
                color: white;
            }

            .dropdown {
                float: left;
                overflow: hidden;
            }

            .dropdown .dropbtn {
                font-size: 17px;
                border: none;
                outline: none;
                color: white;
                padding: 14px 16px;
                background-color: inherit;
                font-family: inherit;
                margin: 0;
            }

            .dropdown-content {
                display: none;
                position: absolute;
                background-color: #f9f9f9;
                min-width: 160px;
                box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
                z-index: 1;
            }

            .add {
                margin-left: 350px;
            }

            .dropdown-content a {
                float: none;
                color: black;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
                text-align: left;
            }

            .topnav a:hover,
            .dropdow n:hover .dropbtn {
                background-color: #555;
                color: white;
            }

            .dropdown-content a:hover {
                background-color: #ddd;
                color: black;
            }

            .dropdown:hover .dropdown-content {
                display: block;
            }

            form {
                width: 600px;

                background: rgb(106, 123, 192);
                padding: 15px 40px 40px;
                border: 1px solid #ccc;
                margin: 50px auto 0;
                border-radius: 5px;
            }
            label {
                display: block;
                margin-bottom: 5px;
            }
            label i {
                color: white;
                font-size: 80%;
                padding: 6px;
            }
            input,
            select {
                border: 2px solid #ccc;
                padding: 6px;
                display: block;
                width: 50%;
                box-sizing: border-box;
                border-radius: 2px;
                background-color: rgb(225, 219, 222);
                border-color: rgb(245, 77, 77);
                margin-left: 30px;
            }
            .row {
                padding-bottom: 10px;
            }
            .form-inline {
                border: 1px white;
                padding: 4px 10px 4px;
                border-radius: 2px;
            }
            .form-inline label,
            .form-inline input {
                display: inline-block;
                width: auto;
                padding-right: 15px;
            }
            .error {
                color: red;
                font-size: 95%;
            }

            input[type="submit"] {
                font-size: 110%;
                font-weight: 100;
                background: rgb(56, 76, 225);
                border-color: #2320dd;

                color: #fff;
                margin-top: 4px;
                cursor: pointer;
            }

            button {
                color: red;
                background-color: blue;
                font-size: large;
            }
        </style>
    </head>
    <body>
        <div>
            <h1 style="text-align: center">Edit Photographer</h1>
            <form action="/save_edit" method="post">
                <div>
                    <div class="cont">
                        <label>Name</label>
                        <input type="text" id="name" name="name" value="<%= user.name%>" />
                    </div>
                    <div class="cont">
                        <label>Mobno</label>
                        <input type="text" id="mobno" name="mobno" value="<%= user.mobno%>" />
                        <label>emaile</label>
                        <input type="text" id="email" name="email" value="<%= user.email%>" />
                    </div>
                    <div class="cont">
                        <label>Address</label>
                        <input type="text" id="address" name="address" value="<%= user.address%>" />
                        <label>Photograph</label>
                        <input type="text" id="amount" name="amount" value="<%= user.amount%>" />
                        <label>date</label>
                        <input type="text" id="date" name="date" value="<%= user.email%>" />
                    </div>
                    <button type="submit">Save</button>
                </div>
            </form>
        </div>
    </body>
</html>
